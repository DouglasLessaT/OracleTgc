# Exemplo de .env para rodar a API dentro do Laravel Homestead (VirtualBox)
#
# Copie para .env dentro da VM ou ajuste seu .env com estes valores quando usar Homestead.
# Credenciais padrão do MySQL no Homestead: homestead / secret

APP_ENV=dev
APP_SECRET=change-this-secret-key-in-production

# MySQL (Homestead: user homestead, password secret, banco oracle_tgc)
DATABASE_URL="mysql://homestead:secret@127.0.0.1:3306/oracle_tgc?serverVersion=8.0&charset=utf8mb4"

# CORS: use a origem do frontend (oracle-tgc.test ou localhost no dev)
CORS_ALLOW_ORIGIN=https://oracle-tgc.test

# JWT
JWT_SECRET=change-this-jwt-secret-in-production

# APIs externas (opcional)
POKEMON_TCG_API_KEY=
SCRYFALL_API=https://api.scryfall.com
